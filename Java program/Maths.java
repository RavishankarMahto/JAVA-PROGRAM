import java.lang.Math;
public class Maths {
    public static void main(String[] args) {
        String languagename1 = "Java";
        String languagename2 = "C++";

        int[] array = {Integer.valueOf(languagename1.length())};
        Integer.valueOf(languagename2.length()), 2};

        for(int i = array.length - 1; i > -1;   i--) {
            long l = Long.valueof(array[i]);
            System.out.println(Math.pow(languagename1.length(), l));

            break;
        }

    }
    
}
